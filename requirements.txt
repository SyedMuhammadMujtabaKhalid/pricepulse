# ============================================================
# PricePulse — Dependencies
# ============================================================
# Pinned versions for reproducibility.
# Install: pip install -r requirements.txt
# ============================================================

# --- Core ---
python-dotenv==1.1.0            # Load .env files into os.environ
pydantic==2.11.3                # Data validation and settings
pydantic-settings==2.9.1        # Environment-based config management

# --- Database ---
sqlalchemy==2.0.41              # ORM + connection management
psycopg2-binary==2.9.10         # PostgreSQL driver (binary for easy install)
alembic==1.16.2                 # Database migrations

# --- Logging ---
structlog==25.4.0               # Structured JSON logging

# --- HTTP / Scraping ---
httpx==0.28.1                   # Async HTTP client (API sources)
playwright==1.52.0              # Browser automation (scraper sources)

# --- Data Processing ---
pandas==2.2.3                   # Analytics and reporting only

# --- Dashboard ---
streamlit==1.45.1               # Interactive dashboard
plotly==6.1.2                   # Interactive charts

# --- API ---
fastapi==0.115.8
uvicorn==0.34.0

# --- Dev / Testing ---
pytest==8.4.0                   # Test framework
pytest-asyncio==1.0.0           # Async test support
pytest-cov==6.2.1               # Coverage reporting
ruff==0.11.13                   # Linter + formatter (replaces flake8/black/isort)
